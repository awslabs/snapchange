[stats]
maximum_new_corpus_size = 5000
minimum_total_corpus_percentage_sync = 99
maximum_total_corpus_percentage_sync = 100

[stats.merge_corpus_timer]
secs = 2
nanos = 0

[stats.stats_sync_timer]
secs = 0
nanos = 500000000

[stats.coverage_sync_timer]
secs = 2
nanos = 0

[redqueen]
entropy_threshold = 8
cores = 10
mutate_by_redqueen_rules = false
